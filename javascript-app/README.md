# Setup

```bash
$ cd javascript-app # if you haven't already changed into this directory
$ npm install
$ node javascript-sample-app.js # or `node --no-warnings javascript-sample-apps` (to remove warning messages in the console)
```

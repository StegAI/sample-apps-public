# Setup

```bash
$ cd python-app # if you haven't already changed into this directory
$ python3 -m venv venv
$ source venv/bin/activate
$ pip install -r requirements.txt
$ python3 python-sample-app.py
```
